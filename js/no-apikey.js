(function($) {
    'use strict';
    $(document).ready(function() {
        setTimeout(function(){
            alert("SmartSearch plugin was unable to find your API KEY. Fix this by setting a valid API KEY in the plugin's settings");
        },1000)
    });
}(jQuery));